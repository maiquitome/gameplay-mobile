import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: 64,
    height: 68,
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 8,
    marginRight: 20,
  },
  image: {
    width: 63,
    height: 67,
    borderRadius: 8,
  }
})
